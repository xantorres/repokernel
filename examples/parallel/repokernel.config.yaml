schemaVersion: 1
projectId: parallel
projectName: RepoKernel Parallel Example

paths:
  epics: epics
  sprints: sprints
  reviews: reviews
  queues: queues
  lanes: lanes
  generated: .repokernel
  registry: .repokernel/registry.json

policies:
  defaultLane: main
  severityFailThreshold: P1

automation:
  allowAutonomousClose: true
