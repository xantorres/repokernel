Add a function `add(a, b)` to `index.js` that returns `a + b`.

Then add a tiny self-test at the bottom of `index.js` that calls `add(2, 3)` and `console.log`s the result.

Keep the file under 30 lines.
