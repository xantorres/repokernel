{
  "name": "repokernel",
  "version": "0.0.0",
  "description": "RepoKernel CLI — run orchestrator, lifecycle commands, validation.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "repokernel": "./dist/index.js",
    "rk": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "12.1.0",
    "gray-matter": "4.0.3",
    "picocolors": "1.1.1"
  },
  "devDependencies": {
    "@repokernel/core": "workspace:*",
    "@types/node": "22.10.2",
    "tsup": "8.3.5",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
